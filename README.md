# C-part
Function which returns the maximum value of the inventory that can be stolen. 


two values are declared initially max_value=0;temp_time=0;

hackflipmart() function is creared.
for the cost and time values array is used.
to access the array for loop is used.
